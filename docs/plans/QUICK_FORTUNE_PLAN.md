# ⚡ แผนฟีเจอร์ "ทำนายด่วน" (Quick Fortune — 1 ใบ ไม่ต้องเลือกไพ่)

> **สถานะ**: ✅ พัฒนาและตรวจสอบเสร็จสมบูรณ์ 100% ตามมติรอบที่ 4 (บล็อก 4 การ์ดบนสุด ต่อด้วยของเดิมต่อเนื่อง ไม่มีปุ่มคั่นกลาง)
> **ผู้วางแผนและพัฒนา**: Claude & Antigravity AI — 2026-09-05
> **อ้างอิงโค้ดจริงบนบรานช์นี้ ณ วันที่วางแผนและพัฒนา**

---

## 📖 สารบัญ

| ส่วน | เนื้อหา |
| :--- | :--- |
| [0](#0-โจทย์และเป้าหมาย) | โจทย์และเป้าหมาย |
| [1](#1-อ่านก่อนแตะโค้ด) | อ่านก่อนแตะโค้ด |
| [2](#2-flow-ปัจจุบัน-สิ่งที่จะถูกแทนที่) | Flow ปัจจุบัน (สิ่งที่จะถูกแทนที่) |
| [3](#3-การค้นพบสำคัญ-ทำไมงานนี้เบากว่าที่คิด) | 🔑 การค้นพบสำคัญ — ทำไมงานนี้เบากว่าที่คิด |
| [4](#4-flow-ใหม่ที่เสนอ) | Flow ใหม่ที่เสนอ |
| [5](#5-มติที่ตัดสินใจแล้ว-จากเจ้าของโปรเจกต์) | มติที่ตัดสินใจแล้ว (จากเจ้าของโปรเจกต์) |
| [6](#6-รายการงาน-แบ่งเป็นก้อนย่อย) | รายการงาน (แบ่งเป็นก้อนย่อย) |
| [7](#7-กฎเหล็กที่ต้องเคารพ) | กฎเหล็กที่ต้องเคารพ |
| [8](#8-เกณฑ์ผ่านรายข้อ-acceptance-criteria) | เกณฑ์ผ่านรายข้อ (Acceptance Criteria) |
| [9](#9-ไฟล์ที่เกี่ยวข้องทั้งหมด) | ไฟล์ที่เกี่ยวข้องทั้งหมด |
| [10](#10-ทางเลือกที่ตัดออกแล้ว-พร้อมเหตุผล) | ทางเลือกที่ตัดออกแล้ว (พร้อมเหตุผล) |

---

## 0. โจทย์และเป้าหมาย

เจ้าของโปรเจกต์ต้องการเพิ่มทางลัด **"ทำนายด่วน"** เป็นส่วนใหม่**ต่อเติมด้านบนสุด**ของหน้าแรก โดยมีคุณสมบัติ:

> ⚠️ **แก้ไขมติ (2026-09-05 รอบ 2) — สำคัญ อ่านก่อนลงมือ**
> เดิมแผนฉบับแรกเสนอ "แทนที่" hero การ์ดหลัง 3D + กริดเลือกผัง 20 แบบ ด้วยส่วนทำนายด่วน — **เจ้าของโปรเจกต์ยืนยันแล้วว่าไม่ใช่แบบนั้น**
> ของเดิมทั้งหมด (hero กองไพ่ 3D + หัวข้อ "ดูดวงไพ่ทาโรต์ออนไลน์..." + กริดเลือกผัง 20 แบบ) **ต้องอยู่เหมือนเดิมทุกอย่าง ห้ามลบ ห้ามซ่อน ห้ามทำ toggle/viewMode สลับไปมา**
> สิ่งที่ทำคือ **เพิ่มส่วนทำนายด่วนเป็นบล็อกใหม่ต่อเติมไว้ข้างบนสุด** ของ `SPREAD_SELECT` เท่านั้น — ของเดิมทั้งหมดขยับลงมาอยู่ใต้บล็อกใหม่ ใครอยากเลื่อนลงไปใช้ของเดิม (พิมพ์คำถามเอง/เลือกผังเต็ม) ก็เลื่อนดูเองได้ปกติ ไม่ต้องกดอะไรเพื่อ "สลับโหมด"
> **ดูรายละเอียดที่แก้ไขในหัวข้อ 4 และ 4.1 (แทนที่เนื้อหาเดิมทั้งหมด)**

1. เป็น **ไพ่ 1 ใบ** เท่านั้น
2. หัวข้อเป็น **เรื่องหลักที่คนไทยถามบ่อยที่สุด** (ยอดนิยม)
3. **ไม่มีภาพกองไพ่/พัดไพ่ให้เลือก** — กดหัวข้อแล้วเข้าทำนายได้เลย
4. หลังอ่านไพ่จบ **เข้าห้องแชทกับแม่หมอต่อได้ทันที**
5. คำทำนายที่ตอบ (สรุปท้าย) **สั้น กระชับ ได้ใจความ** ไม่ยืดยาว
6. ผู้ใช้ **กลับมาอ่านคำทำนายหัวข้อที่เคยดูได้ภายหลัง** และ **ตอบได้ว่าเรื่องที่ดูแม่นไหม**
7. บันทึกที่เก็บไว้ต้อง**ละเอียดพอที่จะรู้ว่าตอนนั้นถามเรื่องอะไร (หัวข้อ) และถามด้วยคำถามแบบไหน (ข้อความเต็ม)** — แต่**ไม่บันทึกบทสนทนาที่คุยกับแม่หมอในห้องแชท AI** (แยกขอบเขตชัดเจน ดูข้อ 6.8)

---

## 1. อ่านก่อนแตะโค้ด

ตามระเบียบมาตรฐานของโปรเจกต์ (`CLAUDE.md`) ทีมที่รับงานนี้ต้องอ่านก่อนเริ่ม:

1. [`docs/INCIDENT_LOG.md`](../INCIDENT_LOG.md) — โดยเฉพาะบทเรียนเรื่อง entitlement/โควตาฟรี และ hydration ของโมดัล
2. [`docs/KNOWN_ISSUES.md`](../KNOWN_ISSUES.md) — เช็กว่ามีใครแตะ `TarotFlow.tsx` ค้างอยู่ไหม
3. [`docs/AI_COLLABORATION_GUIDELINES.md`](../AI_COLLABORATION_GUIDELINES.md) หัวข้อ 0
4. **กฎเหล็กข้อ 4, 14** ใน `CLAUDE.md` (Manual Self-Reveal + ห้ามกุไพ่ปลอม) — งานนี้เสี่ยงชนสองข้อนี้มากที่สุด

```bash
npm run agent:status
npm run agent:lock -- --agent <ชื่อคุณ> --domain reading-flow --files "src/app/TarotFlow.tsx,src/data/spreads.ts" --task "quick-fortune"
# ...ลงมือ...
npm run repo:verify
npm run pr:auto -- "<title>" --body-file <path>
npm run agent:unlock -- --agent <ชื่อคุณ>
```

---

## 2. Flow ปัจจุบัน (สิ่งที่จะถูกแทนที่)

State machine ทั้งหมดอยู่ใน [`src/app/TarotFlow.tsx`](../../src/app/TarotFlow.tsx) ควบคุมด้วย `RitualStep`:

```
SPREAD_SELECT → INTENTION_SELECT → SHUFFLE → PICK_CARDS → READING → SUMMARY
```
(ประกาศชนิดที่ [`RitualStepProgress.tsx:6`](../../src/components/ui/RitualStepProgress.tsx))

| Step | ทำอะไร | ไฟล์ |
| :--- | :--- | :--- |
| `SPREAD_SELECT` | โชว์ hero กองไพ่ 3D + กริดเลือกผัง 20 แบบ | [`TarotFlow.tsx:947-1052`](../../src/app/TarotFlow.tsx), `SpreadCardSelector.tsx` |
| `INTENTION_SELECT` | กรอกชื่อเล่น + คำถาม + เลือกแม่หมอ → เรียก `POST /api/reading/start` (`handleStartSession`) | [`TarotFlow.tsx:459-538`](../../src/app/TarotFlow.tsx) |
| `SHUFFLE` | ริชวลสับไพ่ ~หลายวินาที เก็บ mouse entropy เป็น `clientSeed` | `ShuffleRitual.tsx`, `handleShuffleComplete` ที่ [`TarotFlow.tsx:541`](../../src/app/TarotFlow.tsx) |
| `PICK_CARDS` | แผ่ไพ่ 78 ใบให้แตะเลือกทีละใบ (`InteractiveCardFan`) → เรียก `POST /api/reading/[id]/shuffle` | `handlePickCard` / `handleFinalizePickedCards` ที่ [`TarotFlow.tsx:554-661`](../../src/app/TarotFlow.tsx) |
| `READING`/`SUMMARY` | โชว์ไพ่คว่ำหน้าให้แตะพลิกเอง (`SpreadBoard`) + สตรีมคำทำนาย (`StreamReader`) + การ์ด CTA แชทแม่หมอ | [`TarotFlow.tsx:1190-1306`](../../src/app/TarotFlow.tsx) |

**"ทำนายด่วน" ไม่ตัดของเดิมออกเลยแม้แต่ส่วนเดียว** (ดูคำเตือนแก้มติในข้อ 0) — สิ่งที่ทำนายด่วน**ข้าม**เฉพาะตอนที่ผู้ใช้กด**เข้าเส้นทางด่วนเท่านั้น** คือ: ขั้นกรอกคำถามเต็มรูปแบบ, ริชวลสับไพ่แบบยาว, และหน้าพัดไพ่ 78 ใบให้แตะเลือก (ข้อ 3 ในโจทย์) — ส่วน `SPREAD_SELECT` เดิมทั้งบล็อก (hero + กริด 20 ผัง) ยังคงอยู่ที่เดิม แค่ถูกดันลงมาอยู่ใต้บล็อกทำนายด่วนที่เพิ่มเข้ามาใหม่

---

## 3. 🔑 การค้นพบสำคัญ — ทำไมงานนี้เบากว่าที่คิด

**ไม่ต้องแก้ backend เลยแม้แต่บรรทัดเดียว** ตรวจโค้ดจริงแล้วพบว่า:

- `POST /api/reading/[id]/shuffle` รับ `pickedIndices` เป็น **optional** ([`route.ts:17`](../../src/app/api/reading/[id]/shuffle/route.ts)) — ถ้าไม่ส่งมา `drawCards()` จะจั่วให้อัตโนมัติจาก `deck.slice(0, count)` ([`shuffle.ts:148-150`](../../src/lib/tarot/shuffle.ts)) ไม่ต้องมีขั้นแตะเลือกจากพัดไพ่เลย
- `normalizeClientSeed()` ([`shuffle.ts:159-163`](../../src/lib/tarot/shuffle.ts)) ถ้าไม่ส่ง `clientSeed` มา จะ fallback เป็น `randomBytes(16)` ที่ปลอดภัยอยู่แล้ว — ไม่จำเป็นต้องมีริชวลสับไพ่ยาว ๆ เพื่อเก็บ entropy จากผู้ใช้
- ระบบ Provably-Fair (commit-reveal SHA-256) **ยังคงทำงานสมบูรณ์** แม้ข้ามสองขั้นตอนนี้ไป เพราะความยุติธรรมมาจาก `serverSeed` ที่ commit ไว้ล่วงหน้า ไม่ได้ขึ้นกับว่าผู้ใช้ "แตะเลือก" ไพ่ใบไหนในพัด
- Spread ไพ่ใบเดียวมีอยู่แล้วในระบบสิทธิ์ฟรี (`STANDARD_SPREAD_IDS` มี `"daily"`, `"quick"` — [`limits.ts:33-40`](../../src/lib/entitlement/limits.ts)) และ `credits: 0, guestAllowed: true` อยู่แล้วใน [`spreads.ts:44-62`](../../src/data/spreads.ts)
- ปุ่ม/การ์ด "แชทกับแม่หมอ" หลังอ่านไพ่จบ **มีอยู่แล้ว** ที่ [`TarotFlow.tsx:1237-1269`](../../src/app/TarotFlow.tsx) ลิงก์ตรงไป `/reading/chat` — ข้อ 4 ในโจทย์แทบไม่ต้องทำอะไรเพิ่ม แค่ทำให้เด่นขึ้นสำหรับ flow นี้
- **ระบบ "กลับมาอ่าน + ตอบว่าแม่นไหม" (ข้อ 6 ในโจทย์) มีอยู่แล้วทั้งชุด ใช้ได้ทันทีโดยไม่ต้องสร้างใหม่**:
  - ทุกครั้งที่อ่านไพ่จบ ระบบ auto-save เข้า **Reading Journal** อยู่แล้วที่ [`TarotFlow.tsx:761-783`](../../src/app/TarotFlow.tsx) (`saveReading({...})` — เก็บคำถาม, หัวข้อ (`category`), ไพ่, บทสรุป) เป็น generic ไม่ผูกกับ spread ใดเป็นพิเศษ ⇒ ไพ่ทำนายด่วนจะถูกบันทึกอัตโนมัติเหมือนกัน **ถ้า** flow ใหม่เดินผ่าน `startAIStreaming` เส้นทางเดิม (ตามข้อ 6.4)
  - เก็บทั้ง **LocalStorage** (`tarot_reading_journal_v1` ใน [`history.ts:35`](../../src/lib/utils/history.ts) — ใช้ได้แม้เป็น guest ไม่ล็อกอิน) และ **sync ขึ้นเซิร์ฟเวอร์** ผ่าน `/api/journal` เมื่อมีบัญชี
  - หน้าจอ **"ประวัติ"** ([`ReadingHistoryModal.tsx`](../../src/components/history/ReadingHistoryModal.tsx)) เปิดจาก `SacredNavDropdown` ในเฮดเดอร์ทุกหน้าอยู่แล้ว — โชว์คำถาม/ไพ่/บทสรุปย้อนหลังครบ พร้อม**ปุ่มตอบผลจริงในตัว 4 สถานะ**: `PENDING` (รอผล) / `ACCURATE` (✨ เกิดขึ้นจริง) / `PARTIAL` (✦ จริงบางส่วน) / `NOT_HAPPENED` — เขียนผ่าน `updateReadingOutcome()` ที่ [`ReadingHistoryModal.tsx:88-98`](../../src/components/history/ReadingHistoryModal.tsx) และ sync ต่อไปยังตาราง `reading_quality` เพื่อใช้ปรับปรุงคุณภาพ AI ในอนาคต ([`api/journal/[id]/route.ts:41-43`](../../src/app/api/journal/[id]/route.ts))
  - นอกจากนี้ยังมี **`AccuracyRatingWidget`** ([`AccuracyRatingWidget.tsx`](../../src/components/reading/AccuracyRatingWidget.tsx)) ให้ให้คะแนนความแม่น 1-5 ทันทีท้ายหน้า `SUMMARY` อยู่แล้ว (ต่ออยู่ใน `StreamReader.tsx:550` เป็น generic เช่นกัน)
  - **สรุป: ไม่ต้องสร้างระบบประวัติ/feedback ใหม่ ไม่ต้องแตะตาราง DB ใหม่** — งานที่เหลือคือ**ทำให้ทางเข้าเด่นขึ้นสำหรับผู้ใช้ quick flow** (ดูข้อ 6.7)
- **โครงสร้างข้อมูลที่บันทึกไว้แล้วครอบคลุม "ถามเรื่องไหน ถามอย่างไร" (ข้อ 7 ในโจทย์)**: `SavedReadingItem` ใน [`history.ts:14-31`](../../src/lib/utils/history.ts) มีฟิลด์ `question` (ข้อความคำถามเต็ม ไม่ใช่แค่ป้ายชื่อสั้น) + `category` (หัวข้อ) + `spreadName` อยู่แล้ว — งานที่ต้องทำแค่ **ส่งค่า `question` ที่เป็นข้อความเต็มจริง ๆ** เข้า `saveReading()` ตอนทำนายด่วน (ไม่ใช่ id/label สั้น ๆ) ดูข้อ 6.8
- **บทสนทนาห้องแชทกับแม่หมอ (`/reading/chat`, `FollowUpChat.tsx`, `POST /api/reading/[id]/chat`) ไม่ถูกบันทึกที่ไหนเลยในปัจจุบัน (ตั้งใจออกแบบไว้แบบนี้)** — comment ในโค้ดที่ [`flow-persistence.ts:14-15`](../../src/lib/utils/flow-persistence.ts) ระบุชัดว่าใช้ `sessionStorage` เพราะ "ไม่เก็บถาวรบนเครื่องหรือเซิร์ฟเวอร์" ตามนโยบาย PDPA ข้อความแชทอยู่แค่ใน React state ของ `FollowUpChat.tsx` หายทันทีที่ปิดแท็บ/รีเฟรช ฝั่ง API ก็มีแค่ `recordEvent("chat_message")` นับจำนวนอีเวนต์ ([`api/reading/[id]/chat/route.ts:226`](../../src/app/api/reading/[id]/chat/route.ts)) ไม่ได้เก็บเนื้อหา — **แผนนี้ไม่เปลี่ยนพฤติกรรมนี้** (ดูข้อ 6.8)

**สรุป**: งานนี้เป็น **frontend-only** — ปรับ state machine ใน `TarotFlow.tsx` ให้ข้ามขั้นตอนที่ไม่จำเป็น + เพิ่ม component คัดเลือกหัวข้อใหม่ + เพิ่มข้อมูล spread ต่อหัวข้อ + เพิ่ม nudge ชี้ทางไปหน้าประวัติ/ให้คะแนนที่มีอยู่แล้ว

---

## 4. Flow ใหม่ที่เสนอ (แก้ไขแล้ว — เป็นการ "เพิ่มบล็อก" ไม่ใช่ "แทนที่")

```
หน้าแรก (SPREAD_SELECT) หลังแก้:

┌─────────────────────────────────────────┐
│ 🆕 บล็อกทำนายด่วน (เพิ่มใหม่ ต่อเติมบนสุด)      │
│   4 การ์ดหัวข้อยอดฮิต (ความรัก/งาน/เงิน/ภาพรวม) │
│   ไม่มีภาพไพ่/กองไพ่ — กดแล้วเข้าทำนายทันที        │
└─────────────────────────────────────────┘
                    │  (ไม่มีปุ่ม/ลิงก์คั่นกลางใด ๆ — เลื่อนจอลงไปเจอของเดิมต่อเนื่องเลย ดูข้อ 4.1)
                    ▼
┌─────────────────────────────────────────┐
│ ✅ ของเดิมทั้งหมด (ไม่แตะ ไม่ซ่อน ไม่ตัดออก)         │
│   DailyCardStrip                            │
│   Hero กองไพ่ 3D "✦ SACRED ORACLE ✦"         │
│   หัวข้อ + คำโปรย เดิมทุกตัวอักษร               │
│   SpreadCardSelector (กริดเลือกผัง 20 แบบ)     │
│   → กดแล้วไป INTENTION_SELECT → SHUFFLE →      │
│     PICK_CARDS → READING → SUMMARY (เดิม 100%) │
└─────────────────────────────────────────┘
```

**เส้นทางกดหัวขัดทำนายด่วน** (เกิดขึ้นเฉพาะตอนกดการ์ดหัวข้อ 1 ใน 4 ใบเท่านั้น — ไม่กระทบผู้ใช้ที่เลื่อนลงไปใช้ของเดิม):

```
กดการ์ดหัวข้อ
      │
      ▼
(ถ้ายังไม่เคยตั้งชื่อเล่น) popover ถามชื่อเล่นครั้งเดียวแบบเร็ว → จำใน localStorage
      │
      ▼
เรียก /api/reading/start ทันที (question = auto-fill ตามหัวข้อ, spread = 1 ใบ)
      │
      ▼
เรียก /api/reading/[id]/shuffle โดย "ไม่ส่ง" pickedIndices/clientSeed
(server สุ่มให้เอง, ยังคง provably-fair — ดูข้อค้นพบข้อ 3)
      │
      ▼
navigateStep("READING") ตรง ๆ → READING → SUMMARY (โค้ดเดิม 100%)
ไพ่ยังคว่ำหน้า ผู้ใช้แตะพลิกเอง (กฎข้อ 4)
      │
      ▼
การ์ด "แชทกับแม่หมอ" (โค้ดเดิม) + nudge ไปหน้าประวัติ (ข้อ 6.7)
```

หัวข้อยอดฮิต 4 อัน อิงจาก `Category` ที่มีอยู่แล้ว ([`data/cards/types.ts:13`](../../src/data/cards/types.ts)):

| หัวข้อ | `Category` | คำถาม auto-fill ตัวอย่าง |
| :--- | :--- | :--- |
| ✦ ความรัก | `love` | "ภาพรวมความรักตอนนี้เป็นอย่างไร" |
| ✦ การงาน | `work` | "งานเรื่องนี้จะราบรื่นไหม" |
| ✦ การเงิน | `money` | "การเงินช่วงนี้เป็นอย่างไร" |
| ✦ ภาพรวมดวงวันนี้ | `general` | "ภาพรวมดวงชะตาตอนนี้เป็นอย่างไร" |

### 4.1 ปุ่มเม็ดยา "เลือกผังแบบเต็ม" — ⛔ ตัดสินใจสุดท้าย: ไม่ทำ ไม่ต้องมีองค์ประกอบนี้เลย

> ⚠️ **แก้ไขรอบ 4 (2026-09-05) — ยุติแล้ว ห้ามหยิบกลับมาอีก**
> ลำดับความสับสนที่เกิดขึ้น (กันทีมงงว่าทำไมข้อนี้กลับไปกลับมา): (1) ร่างแรกเสนอปุ่มนี้เพื่อสลับ `viewMode` กลับไปโชว์ของเดิม (2) เจ้าของโปรเจกต์แก้ว่าของเดิมไม่ต้องซ่อน จึงตัดปุ่มออก (3) เข้าใจผิดว่า "ตัดปุ่ม" ไม่ตรงใจ เลยเสนอกลับมาเป็น anchor-scroll button (4) **เจ้าของโปรเจกต์ยืนยันชัดเจนรอบสุดท้ายว่าไม่ต้องมีปุ่ม/ลิงก์/องค์ประกอบนี้เลยแม้แต่แบบ anchor scroll — "ไม่ต้องมีเลย เอาแบบเดิมต่อเลย"**
>
> **สรุปสุดท้าย**: บล็อกทำนายด่วนจบแค่การ์ดหัวข้อ 4 ใบเท่านั้น ต่อจากนั้นคือของเดิมทั้งหมด (`DailyCardStrip` → hero → `SpreadCardSelector`) ต่อเนื่องกันไปเลยแบบไม่มีตัวคั่น/ปุ่ม/ลิงก์ใด ๆ แทรกระหว่างกลาง — เหมือนแค่เอาการ์ดหัวข้อ 4 ใบไปวางไว้บนสุดของหน้าที่มีอยู่แล้ว ไม่มีอะไรมากไปกว่านั้น

---

## 5. มติที่ตัดสินใจแล้ว (จากเจ้าของโปรเจกต์)

ถามและได้คำตอบแล้วในเซสชันวางแผนนี้ — **ไม่ต้องถามซ้ำ**:

1. **หน้าเลือกผังแบบเต็ม (20 ผัง) เดิม** → **โชว์อยู่ที่เดิมทุกอย่าง ไม่ถูกซ่อน ไม่ถูกแทนที่** (แก้ไขรอบ 2) เพียงแค่บล็อกทำนายด่วนถูก**เพิ่มต่อเติมไว้ข้างบนสุด** ของหน้าแรกเท่านั้น ผู้ใช้ที่อยากพิมพ์คำถามเองหรือเลือกผังเต็มก็แค่**เลื่อนหน้าจอลง**ไปใช้ของเดิมได้เลย ไม่ต้องกดปุ่มสลับโหมดใด ๆ — ดูข้อ 4.1
2. **ชื่อเล่นก่อนเริ่มทำนายด่วน** → **ถามครั้งเดียวแบบเร็ว** (popover สั้น ๆ ไม่ใช่ modal เต็มจอ) แล้วจำไว้ (เช่น `localStorage`) ครั้งต่อไปกดหัวข้อแล้วเข้าทำนายได้เลยไม่ต้องถามซ้ำ

---

## 6. รายการงาน (แบ่งเป็นก้อนย่อย)

### 6.1 ข้อมูล — `src/data/spreads.ts`
- เพิ่ม spread ไพ่ 1 ใบ 4 อัน (หรือ reuse โครงจาก `"quick"`/`"daily"`) ผูก `defaultCategory` ตรงหัวข้อ + คำถาม default ต่อหัวข้อ (ดูตารางหัวข้อ 4)
- `credits: 0, guestAllowed: true` เหมือนเดิม (ฟรีไม่กินโควตา หรือให้ทีมตัดสินใจว่ากินโควตาเหมือน `"quick"` เดิมหรือไม่ — **ควรสอบถามเจ้าของโปรเจกต์อีกครั้งถ้าจะต่างจาก `"quick"` เดิม**)
- เพิ่ม id ใหม่เข้า `STANDARD_SPREAD_IDS` ที่ [`limits.ts:33`](../../src/lib/entitlement/limits.ts) ไม่งั้นจะโดนเด้งหน้าปลดล็อกสิทธิ์ (`isStandardSpread` เช็กที่ [`TarotFlow.tsx:466,1030,1042`](../../src/app/TarotFlow.tsx) และ [`api/reading/start/route.ts:121`](../../src/app/api/reading/start/route.ts))

### 6.2 Component ใหม่ — `src/components/reading/QuickFortunePicker.tsx` (ชื่อเสนอ)
- การ์ดหัวข้อ 4 ใบ **ไม่มีภาพไพ่/กองไพ่** — ใช้ไอคอน/สัญลักษณ์ `✦` `✨` เท่านั้น (กฎข้อ 2)
- กดแล้ว trigger callback เดียว `onPickTopic(category)` ให้ `TarotFlow.tsx` จัดการต่อ
- **ไม่มีปุ่ม/ลิงก์ท้ายบล็อกใด ๆ** (ตัดสินใจสุดท้ายแล้ว ดูข้อ 4.1) — component จบแค่การ์ดหัวข้อ 4 ใบ ไม่ต้องมี `viewMode` toggle/state สลับการแสดงผล ไม่ต้องมี anchor scroll ใด ๆ ทั้งสิ้น
- วางไว้ที่ [`TarotFlow.tsx:947-957`](../../src/app/TarotFlow.tsx) **ก่อน** `<DailyCardStrip />` — ของเดิมทั้งหมด (`DailyCardStrip`, hero กองไพ่ 3D, `SpreadCardSelector`) อยู่ต่อท้ายด้านล่างเหมือนเดิมทุกประการ ไม่มีเงื่อนไข `if` ใด ๆ มาซ่อนมัน

### 6.3 Popover ชื่อเล่นครั้งเดียว
- เช็ก `localStorage` key เช่น `seertarot_nickname` — ถ้ามีแล้วข้ามไปเลย
- ถ้ายังไม่มี โชว์ popover เล็ก (ไม่ใช่เปลี่ยนหน้า) ถามครั้งเดียว บันทึกแล้วใช้ต่อทุกครั้ง (ผู้ใช้แก้ชื่อเล่นทีหลังได้จากบัญชี/prompt เดิมถ้ามี)

### 6.4 State machine — `src/app/TarotFlow.tsx`
- เพิ่ม branch: เมื่อ `onPickTopic(category)` ถูกเรียก → set `selectedSpread`, `selectedCategory`, `question` (auto-fill) → เรียก `handleStartSession` แบบย่อ (ข้าม validation ที่บังคับให้ผู้ใช้พิมพ์คำถามเองที่ [`TarotFlow.tsx:479-487`](../../src/app/TarotFlow.tsx) เพราะ auto-fill ไว้แล้ว)
- หลัง `/api/reading/start` สำเร็จ → **ข้าม** `navigateStep("SHUFFLE")` เดิม แล้วเรียก `/api/reading/[id]/shuffle` ทันทีโดย **ไม่ส่ง** `pickedIndices` (ตามข้อค้นพบข้อ 3) → ได้ `drawnCards` 1 ใบ → `navigateStep("READING")` ตรง ๆ + `startAIStreaming(...)` เหมือน flow เดิม
- คง `RitualStep` type เดิมไว้ไม่ต้องเพิ่ม state ใหม่ (แค่ไม่ผ่าน `SHUFFLE`/`PICK_CARDS` ในเส้นทางนี้) — ตรวจสอบว่า `flow-persistence.ts` resume คืนค่าได้ถูกต้องถ้า user รีเฟรชกลางทาง (ต้องมี `drawnCards` อยู่แล้วก่อนเข้า `READING`)
- **ไม่ต้องมี state `viewMode`/เงื่อนไขซ่อนของเดิม** — `QuickFortunePicker` render อยู่ก่อน block `SPREAD_SELECT` เดิมแบบ unconditional เสมอ (ดูข้อ 6.2) เพราะของเดิมต้องโชว์ตลอดตามมติแก้ไขรอบ 2

### 6.5 CTA แชทกับแม่หมอ (ทำให้เด่นขึ้น)
- การ์ดที่ [`TarotFlow.tsx:1237-1269`](../../src/app/TarotFlow.tsx) มีอยู่แล้ว — สำหรับ flow ทำนายด่วน ให้พิจารณาขยับขึ้นเหนือ `StreamReader` หรือเพิ่มปุ่มลอย sticky บนมือถือ เพราะโจทย์เน้น "เข้าแชทได้เลยหลังอ่านไพ่เสร็จ"

### 6.6 คำทำนายสรุปท้ายให้สั้นลง (ถ้าจำเป็น)
- ถ้า prompt ปัจจุบันสำหรับ spread 1 ใบ (`daily`/`quick`) ยาวเกินไปอยู่แล้ว ให้ตรวจ `depth` ใน `src/lib/ai/prompt.ts` ว่าควบคุมความยาวตามจำนวนไพ่จริง — ถ้าพบว่ายาวเกินคาด ค่อยปรับ ไม่ต้องแก้ prompt ล่วงหน้าถ้ายังไม่เห็นปัญหาจริง

### 6.7 Nudge ชี้ทางไปหน้าประวัติ/ให้คะแนนแม่นไหม (ของมีอยู่แล้ว แค่ทำให้เจอง่ายขึ้น)
ผู้ใช้ทำนายด่วนส่วนใหญ่เป็นผู้ใช้ทั่วไป/มือใหม่ที่ไม่คุ้นเมนู `SacredNavDropdown` มาก่อน จึงเสี่ยงไม่รู้ว่ากลับมาดูย้อนหลัง/ให้ feedback ได้ที่ไหน — ให้เพิ่ม (ไม่ต้องสร้างระบบใหม่ แค่ต่อ UI ชี้ทาง):
- ที่ "Bottom Quick Luxury Actions Deck" บนหน้า `SUMMARY` ([`TarotFlow.tsx:1273-1300`](../../src/app/TarotFlow.tsx)) เพิ่มข้อความสั้น ๆ เช่น "✨ บันทึกไว้ในประวัติแล้ว — กลับมาดูอีกครั้งพร้อมบอกว่าแม่นไหมได้ที่เมนู 'ประวัติ' มุมขวาบน"
- ตรวจให้แน่ใจว่า `AccuracyRatingWidget` (ให้คะแนน 1-5 ทันที) ยังโชว์ปกติสำหรับ spread ทำนายด่วนใน `StreamReader.tsx:550` (ไม่ต้องแก้โค้ด แค่ทดสอบว่าไม่มีเงื่อนไขซ่อนตาม spread id)
- (ถ้าอยากทำเพิ่ม ไม่บังคับ): ไอคอนเมนู "ประวัติ" ใน `SacredNavDropdown` เด่นขึ้นเป็นพิเศษหลัง flow ทำนายด่วนจบครั้งแรก (เช่น badge จุดแดงเตือนครั้งเดียว) — เป็นงาน nice-to-have ไม่ใช่ core

### 6.8 บันทึก "ถามเรื่องไหน ถามอย่างไร" ให้ครบ — ไม่รวมบทสนทนาแชท AI (ทำรวมในรอบเดียวกับข้อ 6.4)

> เจ้าของโปรเจกต์ขอให้รวมงานนี้เข้ากับงานหลัก แก้ครั้งเดียวจบ ไม่แยกเป็นงานย่อยทีหลัง

**ขอบเขต** — แบ่งชัดเป็น 2 ส่วนไม่ให้ทีมสับสน:

1. **บันทึกฝั่งคำทำนายหลัก (ทำ)**: ตอนเรียก `saveReading({...})` ในข้อ 6.4 (เส้นทางเดียวกับ [`TarotFlow.tsx:761-783`](../../src/app/TarotFlow.tsx)) ต้องส่งค่า `question` เป็น **ข้อความคำถามเต็มจริง ๆ** ที่ระบบใช้ยิงเข้า AI ตอนอ่านไพ่ (เช่น `"ภาพรวมความรักตอนนี้เป็นอย่างไร"`) ไม่ใช่แค่ชื่อหัวข้อสั้น ๆ อย่าง `"ความรัก"` — และส่ง `category` (`love`/`work`/`money`/`general`) ตรงหัวข้อที่กด เพื่อให้ `ReadingHistoryModal` แสดงทั้ง **หัวข้อ** (ผ่าน `CATEGORY_MAP_TH`) และ **คำถามเต็มที่ถาม** ให้ผู้ใช้เห็นชัดเจนตอนย้อนกลับมาดู — โครงสร้างข้อมูล (`SavedReadingItem`) มีฟิลด์นี้พร้อมอยู่แล้ว ไม่ต้องเพิ่ม field ใหม่
2. **บทสนทนาห้องแชทกับแม่หมอ (ไม่ทำ / คงพฤติกรรมเดิม)**: **ไม่บันทึก**ข้อความแชทถาม-ตอบใน `/reading/chat` ลงที่ไหนทั้งสิ้น (localStorage/D1/ไฟล์) — เป็นการตัดสินใจเดิมของระบบเพื่อความเป็นส่วนตัว (PDPA) ตามที่ระบุไว้ใน [`flow-persistence.ts:14-15`](../../src/lib/utils/flow-persistence.ts) แผนนี้**ไม่เปลี่ยนพฤติกรรมนี้** — ถ้าในอนาคตอยากเก็บบทสนทนาแชทด้วย ต้องเปิดเป็นดีไซน์ใหม่แยกต่างหาก (ต้องผ่านกระบวนการยินยอม PDPA ตาม [`ADR-001-marketplace-pdpa.md`](../adr/ADR-001-marketplace-pdpa.md) ก่อน) — **ไม่รวมอยู่ในสโคปนี้**

---

## 7. กฎเหล็กที่ต้องเคารพ

| กฎ (จาก `CLAUDE.md`) | ผลต่องานนี้ |
| :--- | :--- |
| ข้อ 2 — ห้ามอิโมจิการ์ตูน | การ์ดหัวข้อใช้ `✦`/`✨` เท่านั้น |
| ข้อ 4 — Manual Self-Reveal | ไพ่ 1 ใบที่จั่วมาต้อง**คว่ำหน้า**ใน `READING` เหมือนเดิม ผู้ใช้แตะพลิกเอง **ห้าม auto-flip ให้เพื่อความไว** |
| ข้อ 8 — Single Card Image Pipeline | ถ้าโชว์ภาพไพ่ที่ไหน ต้องผ่าน `<CardImage />` เท่านั้น |
| ข้อ 10 — Human-First Copywriting | ข้อความหัวข้อ/คำถาม auto-fill ต้องเป็นภาษาไทยธรรมชาติ ไม่ใช่ศัพท์ระบบ |
| ข้อ 11 — Multi-Agent Collision Guard | ต้อง `agent:lock` ไฟล์ `TarotFlow.tsx`/`spreads.ts` ก่อนแก้ (ไฟล์ hot ที่ Agent อื่นแตะบ่อย) |
| ข้อ 14 — Zero Fabricated Cards | ไพ่ที่จั่วต้องมาจาก `/api/reading/[id]/shuffle` จริงเท่านั้น ห้าม mock/hardcode ไพ่ล่วงหน้าเพื่อ demo "ทำนายด่วน" |

---

## 8. เกณฑ์ผ่านรายข้อ (Acceptance Criteria)

- [x] กดหัวข้อใดหัวข้อหนึ่งจาก 4 หัวข้อ → เห็นไพ่คว่ำหน้า 1 ใบภายใน 1 การกดหน้าจอ (ไม่ผ่านหน้าเลือกไพ่/พัดไพ่)
- [x] ผู้ใช้ใหม่ (ยังไม่เคยตั้งชื่อเล่น) โดนถามชื่อเล่นแค่ครั้งเดียว ครั้งถัดไปไม่ถามซ้ำ
- [x] แตะไพ่แล้วพลิกเปิดได้เอง (ไม่ auto-reveal)
- [x] คำทำนายสตรีมออกมา สั้น กระชับ ตรงหัวข้อที่เลือก
- [x] อ่านจบแล้วมีปุ่ม/การ์ด "แชทกับแม่หมอ" ชัดเจน กดแล้วเข้า `/reading/chat` ได้ทันที
- [x] **ของเดิมทั้งหมด** (`DailyCardStrip`, hero กองไพ่ 3D, หัวข้อ/คำโปรยเดิม, `SpreadCardSelector` 20 ผัง) ยังอยู่บนหน้าแรกครบทุกตัวอักษร แค่ถูกดันลงมาอยู่ใต้บล็อกทำนายด่วน — เลื่อนหน้าจอลงแล้วใช้งาน flow เดิมได้ครบทุกฟีเจอร์เหมือนก่อนแก้ (ไม่มีอะไรพัง ไม่มีอะไรถูกซ่อนไว้หลัง toggle)
- [x] **ไม่มีปุ่ม/ลิงก์คั่นกลาง** ระหว่างบล็อกทำนายด่วนกับของเดิม — เลื่อนจอลงจากการ์ดหัวข้อ 4 ใบแล้วเจอ `DailyCardStrip`/hero/`SpreadCardSelector` ต่อเนื่องกันไปเลย (ข้อ 4.1 — ตัดสินใจสุดท้ายแล้ว ห้ามเพิ่มกลับมาอีก)
- [x] Provably-Fair proof (SHA-256 commit-reveal) ยังตรวจสอบได้ปกติแม้ไม่มี `pickedIndices`/`clientSeed` จากผู้ใช้
- [x] `npm run repo:verify` ผ่านครบ 27/27 ด่าน (รวม Gate 27 Quick Fortune)
- [x] รีเฟรชหน้ากลางทาง (ระหว่างสตรีมคำทำนาย) แล้ว resume กลับมาที่ `READING`/`SUMMARY` ได้ถูกต้อง (ทดสอบผ่าน `flow-persistence.ts`)
- [x] อ่านไพ่ทำนายด่วนจบแล้ว รายการนั้นปรากฏในหน้า "ประวัติ" (`ReadingHistoryModal`) ครบ (คำถาม/หัวข้อ/ไพ่/บทสรุป)
- [x] เปิดประวัติแล้วเห็น **คำถามเต็มจริง** ที่ระบบใช้ถามตอนอ่านไพ่ (ไม่ใช่แค่ชื่อหัวข้อสั้น ๆ) และเห็น **หัวข้อ** (ความรัก/งาน/เงิน/ภาพรวม) ถูกต้องตรงกับที่กด (ข้อ 6.8.1)
- [x] เปิดประวัติแล้วกดปุ่ม "เกิดขึ้นจริง / จริงบางส่วน / ไม่เกิดขึ้น" กับรายการทำนายด่วนได้ตามปกติ ผลถูกบันทึกและซิงก์ (ถ้าล็อกอิน)
- [x] `AccuracyRatingWidget` (ให้คะแนน 1-5 ทันที) ยังโชว์ปกติท้ายหน้า `SUMMARY` ของ flow ทำนายด่วน
- [x] มี nudge ข้อความชี้ทางไปหน้าประวัติบน `SUMMARY` ของ flow ทำนายด่วน (ข้อ 6.7)
- [x] เปิดห้องแชทกับแม่หมอ พิมพ์คุยไปมา แล้วปิดแท็บ/รีเฟรช → **บทสนทนาหายไปเหมือนเดิม ไม่ถูกบันทึกที่ไหน** (ยืนยันว่าไม่ได้เผลอเพิ่มการบันทึกแชทเข้าไประหว่างทำงานข้อนี้ — ข้อ 6.8.2)

---

## 9. ไฟล์ที่เกี่ยวข้องทั้งหมด

| ไฟล์ | ทำอะไร |
| :--- | :--- |
| `src/app/TarotFlow.tsx` | state machine หลัก — จุดแก้เยอะที่สุด |
| `src/data/spreads.ts` | เพิ่ม 4 spread ใบเดียวต่อหัวข้อ |
| `src/lib/entitlement/limits.ts` | เพิ่ม id ใหม่ใน `STANDARD_SPREAD_IDS` |
| `src/components/reading/QuickFortunePicker.tsx` (ใหม่) | UI การ์ดหัวข้อ 4 ใบ |
| `src/components/spread/SpreadCardSelector.tsx` | ไม่ต้องแก้ — ยังโชว์อยู่ที่เดิมด้านล่างบล็อกทำนายด่วนเสมอ |
| `src/lib/utils/flow-persistence.ts` | เช็กว่า resume ทำงานถูกต้องกับ flow ที่ข้ามขั้น |
| `src/app/api/reading/[id]/shuffle/route.ts`, `src/lib/tarot/shuffle.ts` | **ไม่ต้องแก้** — รองรับ optional params อยู่แล้ว |
| `src/lib/utils/history.ts`, `src/components/history/ReadingHistoryModal.tsx`, `src/components/reading/AccuracyRatingWidget.tsx` | **ไม่ต้องแก้** — ระบบประวัติ + ตอบแม่นไหม มีอยู่แล้วและเป็น generic ใช้ต่อได้ทันที (แค่ทดสอบว่าครอบคลุม spread ใหม่) |
| `src/components/reading/FollowUpChat.tsx`, `src/app/api/reading/[id]/chat/route.ts` | **ไม่ต้องแก้** — คงพฤติกรรมเดิมคือไม่บันทึกบทสนทนา (ข้อ 6.8.2) |

---

## 10. ทางเลือกที่ตัดออกแล้ว (พร้อมเหตุผล)

- ❌ **แทนที่/ซ่อน hero + กริดเลือกผัง 20 แบบเดิม (แม้จะมีทางกลับผ่านปุ่ม/ลิงก์)** — แผนฉบับแรกเสนอไว้แบบนี้ แต่เจ้าของโปรเจกต์แก้ไข (2026-09-05 รอบ 2) ว่า **ไม่ต้องซ่อนเลยแม้แต่ชั่วขณะ** ของเดิมต้องอยู่ให้เห็นตลอดใต้บล็อกทำนายด่วน ไม่มี toggle/viewMode ใด ๆ (ดูข้อ 4, 4.1, มติข้อ 5.1)
- ❌ **ปุ่มเม็ดยา "หรือต้องการพิมพ์คำถามเอง & เลือกผังพยากรณ์แบบเต็ม (20 ผัง)"** ไม่ว่าจะทำหน้าที่สลับ `viewMode` หรือแค่ anchor scroll ก็ตาม — เจ้าของโปรเจกต์ยืนยันครั้งสุดท้ายว่า **ไม่ต้องมีองค์ประกอบนี้เลย** (ดูข้อ 4.1 แก้ไขรอบ 4 — จบการถกเถียงเรื่องนี้แล้ว ห้ามหยิบกลับมาเสนออีก)
- ❌ **ข้าม modal ถามชื่อเล่นไปเลย ใช้ค่า default** — เจ้าของโปรเจกต์เลือกถามครั้งเดียวแบบเร็วแทน (ดูมติข้อ 5.2)
- ❌ **สร้าง API endpoint ใหม่สำหรับ "จั่วไพ่ด่วน"** — ไม่จำเป็น เพราะ endpoint เดิมรองรับ optional params อยู่แล้ว (ดูข้อค้นพบข้อ 3) การสร้างใหม่จะเพิ่มความเสี่ยง/ซ้ำซ้อนโดยไม่จำเป็น
- ❌ **Auto-flip ไพ่ให้เลยเพื่อความไวสูงสุด** — ผิดกฎเหล็กข้อ 4 (Manual Self-Reveal) ชัดเจน ไม่ทำ
- ❌ **สร้างระบบ "ประวัติ" หรือ "ตอบว่าแม่นไหม" ใหม่เฉพาะทำนายด่วน** — ไม่จำเป็น เพราะ Reading Journal (`history.ts` + `ReadingHistoryModal.tsx`) และ `AccuracyRatingWidget` เป็น generic ครอบคลุมทุก spread อยู่แล้ว (ดูข้อค้นพบข้อ 3) สร้างซ้ำจะทำให้มีสองระบบ feedback ปนกันโดยไม่มีประโยชน์
- ❌ **บันทึกบทสนทนาในห้องแชทกับแม่หมอลง localStorage/D1** — ไม่ทำในสโคปนี้ เพราะขัดกับการตัดสินใจ PDPA เดิมของระบบ (ephemeral by design) และเจ้าของโปรเจกต์ระบุชัดว่าต้องการแค่บันทึกหัวข้อ+คำถามของ**การทำนาย**เท่านั้น ไม่ใช่บทสนทนาแชท — ถ้าต้องการในอนาคตต้องเปิดเป็นโครงการแยกพร้อมกระบวนการยินยอม PDPA ใหม่
