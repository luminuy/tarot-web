/**
 * สตับของแพ็กเกจ `server-only` สำหรับสคริปต์ใน `scripts/qa` เท่านั้น
 * ---------------------------------------------------------------------------
 * `server-only` ไม่ใช่แพ็กเกจที่ติดตั้งจริงใน node_modules — Next.js เป็นคน
 * resolve ให้เองตอน build ฉะนั้นไฟล์ที่ `import "server-only"` (เช่น
 * `src/lib/ai/gemini.ts`) จะรันผ่าน `tsx` ตรง ๆ ไม่ได้เลย: ERR_MODULE_NOT_FOUND
 *
 * ทางเลือกที่ **ไม่** เอา:
 *   ✗ ลบ `import "server-only"` ออกจาก gemini.ts — ทิ้งด่านกันเผลอ import
 *     โมดูลที่ถือคีย์ Gemini เข้าไปในบันเดิลฝั่งไคลเอนต์
 *   ✗ ใส่ alias ลง `tsconfig.json` ตรง ๆ — Next.js อ่านไฟล์เดียวกัน ด่านนั้น
 *     จะถูกปิดในแอปจริงไปด้วยโดยไม่มีใครรู้
 *
 * ทางที่เลือก: alias อยู่ใน `tsconfig.scripts.json` ซึ่ง **มีแต่ tsx ของ QA**
 * ที่อ่าน — แอปจริงยังใช้ `server-only` ตัวจริงครบทุกไฟล์เหมือนเดิม
 */
export {};
