/**
 * กุญแจสัปดาห์สำหรับโควตาเปิดไพ่ของสมาชิก
 * ---------------------------------------
 * ตัดสัปดาห์แบบ "ปฏิทิน เริ่มจันทร์ 00:00 เวลาไทย" (ENTITLEMENT_PLAN ข้อ 2)
 * ไม่ใช้เลขสัปดาห์ ISO เพราะมีกับดักคาบปีและไลบรารีคำนวณไม่ตรงกัน
 * 🕗 R-25: เส้นแบ่งวันทั้งหมดมาจาก `@/lib/time/bangkok` ที่เดียว — ห้ามคำนวณเองที่นี่อีก
 */

/*
 * 🕗 R-25: ทั้งสามฟังก์ชันนี้เคยคำนวณเส้นแบ่งวันเองที่นี่
 * ตอนนี้เป็นเพียงชื่อเรียกของตรรกะใน `@/lib/time/bangkok` ซึ่งเป็นแหล่งความจริงเดียว
 */
import { bangkokDayKey, bangkokWeekKey, bangkokNextMidnightISO } from "@/lib/time/bangkok";

/** คืนวันที่ของวันนี้ตามเวลาไทย เช่น '2026-09-01' */
export const dayKey = bangkokDayKey;

/** คืนวันที่ของวันจันทร์ต้นสัปดาห์ตามเวลาไทย เช่น '2026-08-31' (compat) */
export const weekKey = bangkokWeekKey;

/** เวลาที่โควตารายวันจะรีเซ็ต = เที่ยงคืนถัดไป 00:00 เวลาไทย (ISO string สำหรับส่งให้ UI) */
export const nextResetAt = bangkokNextMidnightISO;
