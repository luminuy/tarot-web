/**
 * ชื่อคุกกี้ของระบบสมาชิก — ไฟล์นี้ต้อง "ไม่พึ่งอะไรเลย" โดยเจตนา
 * ---------------------------------------------------------------------------
 * ทั้งฝั่งเซิร์ฟเวอร์ (route handlers) และฝั่งหน้าเว็บ (`session-hint.ts`) ต้องใช้ชื่อ
 * เดียวกัน ถ้าปล่อยให้ฝั่งหน้าเว็บ import จาก `edge-auth.ts` จะลากโค้ดเซ็นลายเซ็น HMAC
 * และการอ่าน `AUTH_SECRET` เข้าไปในบันเดิลไคลเอนต์โดยไม่จำเป็น
 */

/** คุกกี้เซสชันตัวจริง — httpOnly เสมอ หน้าเว็บอ่านไม่ได้และห้ามพยายามอ่าน */
export const AUTH_COOKIE_NAME = "tarot_auth_session";

/**
 * คุกกี้ "ใบ้" ว่าเครื่องนี้มีเซสชันอยู่ — อ่านได้จาก JavaScript (ไม่ใช่ httpOnly) โดยตั้งใจ
 *
 * คุกกี้เซสชันจริงเป็น httpOnly หน้าเว็บจึงมองไม่เห็น ทุกหน้าที่โหลดจึงต้องยิง
 * `GET /api/auth/me` ถามเซิร์ฟเวอร์ว่า "ใครอยู่" แม้ผู้ชมจะไม่เคยล็อกอินเลยสักครั้ง
 * — คนที่มาจาก Google คือคนกลุ่มนี้เกือบทั้งหมด และนั่นคือคำขอที่ปลุก Worker ทุกครั้ง
 * (หน้า HTML เสิร์ฟจาก KV/asset ได้ แต่ `/api/*` ปลุก Worker เสมอ)
 *
 * ⚠️ คุกกี้นี้ไม่ได้ให้สิทธิ์อะไรทั้งสิ้น เก็บแค่ "1" ไว้บอกหน้าเว็บว่าควรถามหรือไม่
 * การบังคับสิทธิ์ยังตรวจจากคุกกี้เซสชันจริงฝั่งเซิร์ฟเวอร์ที่เดิมทุกประการ
 */
export const AUTH_HINT_COOKIE_NAME = "tarot_has_session";
