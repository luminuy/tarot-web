/**
 * 🎲 เมล็ดสุ่มฝั่งผู้ใช้ — หัวใจของ Provably Fair
 * ===========================================================================
 * ผู้ใช้ต้องเป็นคนกำหนดผลการจั่วร่วมกับเซิร์ฟเวอร์เสมอ ไม่ใช่รับผลที่เซิร์ฟเวอร์สุ่มมาให้อย่างเดียว
 *
 * ⚠️ ห้ามปล่อยให้ค่านี้ว่างแล้วให้เซิร์ฟเวอร์สุ่มแทนเด็ดขาด (บทเรียน: ทั้งเว็บเคยเป็นแบบนั้น
 * เพราะ state ตั้งต้นเป็น "" และ /start ไม่เคยคืน clientSeed กลับมา ทางถอยฝั่งเซิร์ฟเวอร์
 * จึงทำงานทุกครั้งโดยไม่มีใครสังเกต)
 *
 * เดิมฟังก์ชันนี้ซ่อนอยู่ใน `TarotFlow.tsx` ตัวเดียว พอหน้าอื่นต้องเปิดไพ่ผ่านท่อ AI เหมือนกัน
 * จึงยกออกมาไว้ที่นี่ เพื่อไม่ให้แต่ละหน้าคัดลอกสูตรสุ่มไปคนละชุด
 */
export function createClientSeed(): string {
  const bytes = new Uint8Array(32);
  crypto.getRandomValues(bytes);
  return Array.from(bytes, (b) => b.toString(16).padStart(2, "0")).join("");
}
