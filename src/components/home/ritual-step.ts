/**
 * 🪜 ขั้นตอนของพิธีเปิดไพ่ — ชนิดข้อมูลกลางของทั้ง flow
 * ===========================================================================
 *
 * เดิมชนิดนี้อยู่ใน `src/components/ui/RitualStepProgress.tsx` ซึ่งเป็นคอมโพเนนต์
 * แถบความคืบหน้า **ที่ไม่มีหน้าไหนเรนเดอร์แล้ว** (ตัวจริงที่ผู้ใช้เห็นคือหัวข้อประจำขั้น
 * ในแต่ละสเตปของ `TarotFlow`) · รอบ R-31 ลบคอมโพเนนต์นั้นทิ้ง จึงย้ายชนิดมาอยู่ที่นี่
 * เพื่อไม่ให้ต้องเก็บไฟล์คอมโพเนนต์ 132 บรรทัดไว้เพียงเพราะมี `type` ติดอยู่ข้างใน
 *
 * ⚠️ ลำดับใน `STEP_ORDER` ของ `TarotFlow` ต้องเรียงตามชนิดนี้เสมอ
 * (ทิศทางแอนิเมชันขาเข้า/ขาออกคำนวณจากดัชนีในลำดับนั้น)
 */
export type RitualStep =
  | "SPREAD_SELECT"
  | "INTENTION_SELECT"
  | "SHUFFLE"
  | "PICK_CARDS"
  | "READING"
  | "SUMMARY";
