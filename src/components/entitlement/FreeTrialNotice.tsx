"use client";

/**
 * แถบบอกสิทธิ์ล่วงหน้าบนขั้นเลือกผัง — ปิดการแสดงผลเพื่อความสะอาดตาของหน้าแรก (Zero-clutter)
 * ข้อมูลสิทธิ์ย้ายไปแสดงที่ปุ่มกดเริ่มเปิดไพ่ (Just-in-time) และ UserProfileBadge แทน
 */
export function FreeTrialNotice({ onOpenAccess: _onOpenAccess }: { onOpenAccess?: () => void }) {
  return null;
}
